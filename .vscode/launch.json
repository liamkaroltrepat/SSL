{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Depurar archivo actual (GDB)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${fileDirname}",
      "environment": [],
      "externalConsole": false, //a veces me gusta la consola externa
      "preLaunchTask": "Compilar archivo actual (gcc)",
      "MIMode": "gdb",
      "miDebuggerPath": "L:/programas/mysy2/mingw64/bin/gdb.exe",
      "setupCommands": [
        {
          "description": "Habilitar impresión bonita",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
